[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "smart-parking-lot"
version = "0.1.0"
description = "Smart Parking Lot Rate Calculator"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
